const primaryField = 'full_name';
const mediaField = 'image';

const defaultLayouts = {
	table: {
		layout: {
			primaryField,
		},
	},
	grid: {
		layout: {
			primaryField,
			mediaField,
		},
	},
};

export default defaultLayouts;
